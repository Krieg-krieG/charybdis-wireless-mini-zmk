# Как устроена навигация на Charybdis

## Слои (layers)

Всего 8 слоёв, переключаются по-разному:

| № | Слой | Как попасть |
|---|------|-------------|
| 0 | **BASE** — обычная раскладка | всегда |
| 1 | **NUM** — калькулятор (F1–F12, +, ×, ÷) | удерживать `BS` |
| 2 | **NAV** — стрелки, vim-движение, tmux-панели | двойной тап `Space` или удержание `Space` |
| 3 | **SYM** — символы для кода (`~ ^ & ( ) [ ] # $`) | удерживать `LCLK` (левый клик мышью) |
| 4 | **GAME** — WASD + Ctrl/Shift/Alt | двойной тап правого верхнего угла |
| 5 | **EXTRAS** — медиа, BT-девайсы, яркость, макросы | удержание `EXTRAS` (тап-танц) |
| 6 | **SLOW** — медленный указатель | двойной тап `ESC` |
| 7 | **SCROLL** — шарик = скролл | двойной тап `ESC` (второй вариант) |

**Важные детали:**
- `mo` (momentary) — слой активен, пока удерживаешь клавишу, отпустил — вернулся в BASE.
- `to` (toggle) — слой включается/выключается тапом.
- **Space** — тап = пробел, удержание = NAV-слой, **двойной тап** = тоже NAV (но остаётся).
- **LCLK** (левая кнопка мыши, клавиша `C` на нижнем ряду) — тап = клик, удержание = SYM-слой, двойной тап = клик.
- **BS** — тап = backspace, удержание = NUM.
- **EXTRAS** (клавиша `V` + правая нижняя, combo 38+39) — тап = EXTRAS-слой, двойной тап = возврат в BASE.
- **ESC** (combo Q+W слева или I+O справа) — тап = ESC, двойной тап = SLOW-слой.

## homerow-mod (главная фишка)

Клавиши **A S D F** (левая рука) и **J K L ;** (правая рука) — это hold-tap:
- **тап** = буква
- **удержание + клавиша другой руки** = модификатор (A=Cmd, S=Alt, D=Shift, F=Ctrl, J=Ctrl, K=Shift, L=Alt, ;=Cmd)

То есть чтобы нажать `Ctrl+C` — удерживаешь F и жмёшь C. Это называется **homerow-mods**: модификаторы на домашнем ряду, а не на краях.

## Комбо (одновременное нажатие 2 клавиш)

- **Q+W** или **I+O** → ESC
- **F+G** (17+18) → CapsWord (капс на текущее слово)
- **Z+X** → левый клик мыши
- **X+C** → средняя кнопка
- **C+V** → правый клик
- **Z+Z** (0+24) или **P+DEL** (11+35) → bootloader
- **Q+P** (0+11) → очистить BT-список
- **Z+X** (24+25) → предыдущий desktop
- **M+DEL** (34+35) → следующий desktop
- **V+B** (20+21) → Alt+Tab / Ctrl+F10 (hold)
- **B+N** (19+20) → Alt+Grave / Ctrl+F7 (hold)

## Мышь (шарик PMW3610)

- Шарик вращается → курсор движется (скорость 5×5 по умолчанию).
- **SLOW-слой** (6): скорость падает до 2×6 — для точного позиционирования.
- **SCROLL-слой** (7): движение шарика превращается в скролл (Y инвертирован, скорость 1×15).
- Кнопки мыши — комбо на нижнем ряду (Z/X/C/V слева, M/N/DEL справа).

## NAV-слой (vim-стиль)

В NAV:
- **Q W E R T** = стрелки (H J K L), **G** = detach tmux-сессии
- **Y U I O P** = tmux: create/attach, next pane, show sessions, sync panes
- **TAB, MB4, Alt, Shift, Ctrl, MB5** — как есть
- **H J K L** = движение по строкам, **HOME/PG_DN/PG_UP/END**
- **W S D F** = vim-движение (word left/down/up/right)

## Символы (SYM-слой)

Удержание LCLK даёт: `~ $ ^ & ( ) [ ] { } @ #` + vim-макросы (move line up/down, delete line, multicursor).

## Прочее

- **EXTRAS**: яркость Prospector (`pbl`), BT-переключение (0–3), медиа, undo/cut/copy/paste, `git commit -am`, `git push`, `sudo`, shrug, lgtm.
- **NUM**: цифры + F-клавиши (mod-morph: Shift+7 = F7, обычное 7 = +).

Коротко: **тап = буква/действие, удержание = слой/модификатор, двойной тап = быстрый переключатель, пара клавиш = комбо**.
